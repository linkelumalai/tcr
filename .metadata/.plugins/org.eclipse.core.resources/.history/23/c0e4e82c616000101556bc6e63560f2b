package learn.strms;

import java.util.Random;
import java.util.stream.Stream;

public class LimitStream {
	public static void main(String[] args) {
		
		Stream.generate(new Random()::nextInt).limit(10).map(data -> data * 2).forEach(System.out::println);
	}
}
